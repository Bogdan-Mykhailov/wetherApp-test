export enum ErrorType {
  REMOVE_CARD = 'Unable to delete card',
  UPDATE_CARD = 'Unable to update card',
  AUTH = 'Unable to authenticate',
  FETCHING_DATA = 'Error fetching weather data',
  NONE = '',
}
